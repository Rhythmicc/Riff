# Riff 开发准则

本文件是 Riff 项目（原生 macOS SwiftUI/AppKit 启动器）的强制开发约定，任何改动都遵守以下规则。

## 1. 不保留向后兼容

过时的代码直接删除：不加兼容层、不写迁移框架、不留 fallback 分支、不保留仅供测试使用的旧接口。

例外只针对**用户数据**：允许一次性本地转换（如 `chat.json` → SQLite），转换后旧文件改名或丢弃；不允许在应用内维护永久迁移代码。

## 2. 选能满足当前需求的最简单实现

不做预防性抽象，不加多余的配置层，不为“未来可能”的消费者预留接口。接口只有在出现第二个真实调用方时才值得抽象。

## 3. 系统分层长，先跑通最小端到端

每个功能先以最小垂直切片跑通（数据 → 服务 → UI），验证后再扩展。绝不为了未完成的复杂度拆掉能运行的部分。

## 4. 组件保持模块化，关注点分离

遵循现有分层：`Components/`、`Services/AI/`、`Services/Search/`、`Launcher/`、`UI/`。避免出现 god object；`AppModel` 只做协调，不承载具体实现。

## 5. 优先使用成熟的、有人维护的库

优先系统框架（AppKit、Foundation、NaturalLanguage、Security、sqlite3）和活跃维护的依赖（如 swift-markdown-engine）。自己实现仅限高度定制的小算法（如启动器匹配打分），并说明为什么现有库不满足。

## 6. 先翻已有依赖能做什么

在考虑添加新包或自己写之前，先确认现有依赖和系统框架已提供的能力（例如拼音用 CFStringTransform，语言处理先看 NaturalLanguage，不要假设“库里没有”）。

## 7. 架构决策往长了做

协议、表结构、组件 ID、模块边界按长期设计，不接受“先这样以后再换”的临时方案。

注意与第 2、3 条的调和：**决策长期、实现增量**。接口按长期形态定义，代码按最小端到端切片实现。

## 8. 参考成熟产品，用已验证的模式

启动器匹配参考 Raycast/Alfred/Spotlight（前缀 + 缩写 + 拼音 + 使用学习）；第三方组件参考 Raycast Extensions / Alfred Workflows 的 manifest + 子进程协议；AI 服务参考 LiteLLM 式 transport adapter。不从零发明轮子。

## 9. 修复先定位根因，不在症状层面打补丁

- 先复现，再沿数据流追踪到症状产生的确切代码点（例如“只显示 7 条”要追到 `prefix(7)` 这一层，而不是去调行高/窗口大小/滚动条）。
- 在产生问题的层级修复；调 UI、扩容量、加窗口都不算修复。
- 每个 bug 修复必须补一条回归测试，钉住根因行为，防止复发。
- 动新功能前先读完整数据流，确认没有隐藏的截断、默认值或上限在悄悄改数据。
