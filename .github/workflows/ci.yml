name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    # Xcode 26+ is required to compile SwiftUI's Liquid Glass APIs.
    runs-on: macos-26
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Run tests
        run: swift test
